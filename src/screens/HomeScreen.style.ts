import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  modalContainer: {
    backgroundColor: 'white',
    margin: 20,
  },
  modalText: {
    margin: 20,
  },
  searchBar: {
    margin: 10,
  },
});
